# Design reference

Read-only copies of interactive mockups from `docside-experience/07-screen-design/`, vendored here so cloud planning sessions (which only see this repo) can use them. The originals in `docside-experience` are the source of truth — do not edit these copies.

- `comparison-view-mockup.html` (+ `-v2`, `-v3`) — the comparison view. **Founder-preferred visual direction for First Look surfaces** (Morris, 2026-08-03). Relevant to Mission 3.
- `verify-workspace-mockup.html` — the verify workspace. Relevant to Missions 1–2 and the summary → extracted value → original source language trust moment.

Open them directly in a browser; they are self-contained HTML.
